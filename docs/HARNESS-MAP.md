# 🧭 HARNESS MAP — the 13 agentic-engineering primitives → where they live

> One page answering **"where does X live"** for our agentic setup: each primitive → concrete files/paths in THIS repo (+ global `~/.claude/`) with a ground-truth status.
> Verified 2026-07-07 by reading/listing the files (not memory). Status: ✅ solid · ⚠️ partial · 🆕 recently added / landing this commit.

## The map

| # | Primitive (CEO's word) | Where it lives (files/paths — ground-truthed) | Status |
|---|---|---|---|
| 1 | **claude md** (agent constitution/context) | Project: `CLAUDE.md` (deep Claude detail) → `AGENTS.md` (tool-agnostic entry map) → `GEMINI.md` (pointer). Global: `~/.claude/CLAUDE.md` (Atlas operating protocol). Per-tool entry points in `AGENTS.md §8`. | ✅ |
| 2 | **specifications** (locked specs, not vibes) | `.claude/rules/content-factory-law.md` (12 factory laws, file:line-pinned) · `.claude/rules/constitution-quickref.md` (5 Foundation Laws + 8 crystal ethics) · `.claude/rules/crystal-shop-ethics.md` · `docs/ROUTER-CONTRACT.md` · `tmp/kapibara/QUALITY-GATES-SPEC-2026-07-05.md` · `tmp/kapibara/STORYBOARD-ENGINE-DESIGN.md`. Supreme: `C:/Projects/VOLAURA/docs/ECOSYSTEM-CONSTITUTION.md` v1.7. | ✅ |
| 3 | **subagent / skill / hooks** | Subagents: `.claude/agents/*.md` (10: code-reviewer, guardrail-auditor, a11y-scanner, e2e-runner, growth, infra, liveops, product-analyst, researcher, sec). Skills: `.claude/skills/*` (content-critic, humanizer, ui-ux-design, mobile-review, perf-audit, playwright-test, git-workflow) + global `~/.claude/skills/*`. Commands: `.claude/commands/*` (/verify, /build-fix, /e2e, /guardrails, /tdd…). Hooks: `.claude/hooks.json` (6 PostToolUse: tsc, console.log, framer-motion, shame-words, red-hex, partialize; + PreToolUse git reminder) + global `~/.claude/hooks/*.sh` (constitution-guard, honesty-prompt-guard, honesty-stop-guard, secret-stream-guard, spend-cap-guard, save-yusif-messages). | ✅ |
| 4 | **plan / task breakdown** | `memory/marketing-backlog.md` (agile pool: FINISH LINE → SPRINT → DONE → WAITING-CEO) · `docs/EXECUTION-PLAN.md` · `CLAUDE.md` Sprint History table · scheduled one-shots in `.claude/scheduled_tasks.json` · in-session `TaskCreate/TaskUpdate`. | ✅ |
| 5 | **exit criteria / gates** | Factory (`content-factory-law.md` Law 10): `tmp/kapibara/frame_check.mjs` (per-frame, exit 1 = REWORK) · `tmp/kapibara/content_critic.mjs` (whole-clip, `process.exit(shipReady?0:1)`) · `tmp/kapibara/line.mjs` (orchestrates, halts at first REWORK) · CTA-guard `tmp/kapibara/ladder_render.mjs:26 assertBotExists()` (bot must exist before render spend). App: `tsc -b` gate + `.claude/commands/verify.md`. | ✅ |
| 6 | **context management** | Breadcrumb pattern (`CLAUDE.md` → `memory/wip-*.md`) · handoff cards (`memory/COMPACT-HANDOFF-*.md`, `mega-handoff-next-session.md`) · index `~/.claude/projects/.../memory/MEMORY.md` · `memory/glossary.md`. 🆕 `.claude/hooks/pre-compact-dump.sh` (pre-compact state dump — landing this commit). | ⚠️→🆕 |
| 7 | **sandboxing / isolation** | Git worktrees `.claude/worktrees/` + `EnterWorktree`/`ExitWorktree` tools · pipeline isolated from app (`tmp/kapibara/` never touches `src/`, own `.gitignore`) · CI runs on throwaway `ubuntu-latest` runners · secrets isolated (`~/.claude/hooks/secret-stream-guard.sh`, `.env` gitignored, GH Secrets). Noisy scans → sub-agents (`reliable-execution.md` §11). | ⚠️ |
| 8 | **trajectory review** | 🆕 `.claude/skills/retro` (session retro skill — landing now) · `memory/sprint-retro-coordination-2026-06-13.md` (token-efficiency self-audit) · `code-reviewer` agent + `.claude/commands/code-review.md` · factory post-mortems `tmp/kapibara/AUDIT-RESULTS-*.md`, `WORKFLOW-AUDIT-2026-07-05.md`, `ERROR-CATALOG-2026-07-05.md`. | 🆕 |
| 9 | **guardrails** | `.claude/rules/guardrails.md` (10 hard rules) · `.claude/rules/never-delete.md` · `.claude/rules/crystal-shop-ethics.md` · enforced by hooks: `~/.claude/hooks/constitution-guard.sh` (blocks red on Write/Edit) + `hooks.json` red-hex/framer-motion/shame-word greps · `guardrail-auditor` agent + `/guardrails`. Factory palette guard = `frame_check.mjs` (irritating-red HIGH). | ✅ |
| 10 | **verification loops** | Build: `tsc -b` (`hooks.json` PostToolUse + CI). Honesty: `~/.claude/hooks/honesty-prompt-guard.sh` + `honesty-stop-guard.sh` (blocks "готово/done" without a `## Что проверено` tool-receipt section). Protocol: `~/.claude/rules/reliable-execution.md` (gate every step). Media: `content_critic.mjs` rework loop. CI e2e `e2e-production.yml`. `/verify`. | ✅ |
| 11 | **retrieval stack** | **DECISION: no embeddings/RAG — maps + grep chosen deliberately for this repo's scale.** Maps: `AGENTS.md` (master), `docs/FACTORY-MAP.md`, `tmp/kapibara/PROJECT-MAP.md`, `MEMORY.md` index, `memory/glossary.md`, `CLAUDE.md` hot-cache tables. Search: `Grep`/ripgrep + `Glob`. Cache: `.claude/project_qa_index.json`. | ✅ |
| 12 | **agentic CI/CD** | `.github/workflows/`: `kapibara-daily.yml` (3 crons 08:25/10:10/13:00 Baku + gates + auto-publish IG/TikTok, idempotent via `state.json`+`publish_journal`) · `pult-poller.yml` (*/10 min, drains @CreatorBy_bot queue) · `pult-arm.yml` (webhook secret + register, 200/403 probes) · `rebuild-outro.yml` (self-commits `outro.mp4` back to main) · `ci.yml` (lint+build+vitest+e2e) · `e2e-production.yml` (on Vercel deploy) · `deploy-edge-functions.yml` (on CI success) · `sync-to-zeus.yml`. | ✅ |
| 13 | **feedback / iteration loop** | `tmp/kapibara/metrics.mjs` (daily Buffer numbers → `metrics.json`) → `tmp/kapibara/weekly_report.mjs` (Sunday digest to CEO TG, one-shot cron in `scheduled_tasks.json`) → `tmp/kapibara/dashboard.html` (Mini-App). Human-in-loop `/go` gate. 🆕 `tmp/kapibara/EXPERIMENTS.md` (A/B hypothesis log — landing now). Learnings folded into `memory/marketing-backlog.md` DONE + `memory/mistakes.md`. | ⚠️→🆕 |

## Notes (grounding caveats)

- Rows 6, 8, 13 are marked 🆕 because their newest artifacts (`pre-compact-dump.sh`, `skills/retro/`, `EXPERIMENTS.md`) plus `.claude/settings.json` are being written by a parallel agent **in this same commit** — if a path 404s at read time, it is landing now, not missing by design.
- **Nothing here is a gap in the 13.** All 13 primitives EXIST; the problem CEO named was that they were scattered + the map was stale. This file is the retrieval layer that de-scatters them. The only genuinely *thin* areas are isolation depth (row 7 — worktrees exist but most work runs in the main tree) and the metrics feedback loop (row 13 — history is overwritten, no golden-sample regression yet; both tracked in `docs/FACTORY-MAP.md` "не применено").
- Sister maps: `docs/FACTORY-MAP.md` (the media line, stage-by-stage, RU) · `AGENTS.md` (repo entry map) · `MEMORY.md` (memory index).
