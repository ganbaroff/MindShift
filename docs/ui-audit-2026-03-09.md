# MindShift UI Audit — 2026-03-09

**Auditor:** Claude Code
**Build:** 0 errors · 78/78 tests passing
**Scope:** `src/features/` — CSS/layout only, no logic/hooks/store changes

---

## Checklist (per screen)

| # | Check | Result |
|---|-------|--------|
| 1 | Input fields — no icons bleeding into placeholder | ✅ Pass |
| 2 | Touch targets — all buttons/links ≥ 44×44 px | Fixed |
| 3 | Empty states — every list/pool has helpful empty state | ✅ Pass (added in prior sprint) |
| 4 | Overflow — no text clipping, no content cut off at edges | ✅ Pass |
| 5 | Spacing — consistent horizontal padding on all screen edges | ✅ Pass (`px-5` throughout) |
| 6 | Fixed overlays — nothing covers BottomNav (z-index audit) | Fixed |
| 7 | Full height — no large black voids, screens use min-h-screen | Fixed |

---

## Files Changed

### 1. `src/features/auth/AuthScreen.tsx`
**Issue:** `← Use a different email` button had no explicit height — tap target ~20 px (< 44 px min).
**Fix:** Added `min-h-[44px] px-4` to button className.

---

### 2. `src/features/onboarding/OnboardingFlow.tsx`
**Issue:** `BackBtn` had no explicit height — tap target ~24 px (< 44 px min).
**Fix:** Added `min-h-[44px] px-2` to BackBtn button className.

---

### 3. `src/features/focus/FocusScreen.tsx`
**Issue (previously fixed sprint):** Empty state added for zero-task setup screen.
**Issue (this sprint):** Setup screen used `min-h-screen` on the content div, creating a large black void below the "Start Focus →" button (~350 px on 844 px iPhone).
**Fix:** Removed `min-h-screen` from setup wrapper div — content now flows naturally, `pb-28` ensures clearance above BottomNav.

---

### 4. `src/features/audio/AudioScreen.tsx`
**Issue:** Sound Anchor list buttons used `p-2.5` — computed height ~38 px (< 44 px min).
**Fix:** Changed `p-2.5` → `py-3 px-2.5 min-h-[44px]` on each anchor-selection button.

---

### 5. `src/features/progress/ProgressScreen.tsx`
**Issue:** "Generate" insight button used `py-1` — computed height ~22 px (< 44 px min).
**Fix:** Changed `py-1 px-3` → `py-2.5 px-4 min-h-[44px]`.

---

### 6. `src/features/settings/SettingsScreen.tsx`
**Issues found:**
- `Toggle` switch button: `py-2` → height ~36 px
- App Mode buttons: `py-2.5` → height ~40 px
- Focus Style buttons: `py-2.5` → height ~40 px
- Activate trial / Later buttons: `py-2.5` → height ~40 px
- Delete confirm / Cancel buttons: `py-2.5` → height ~40 px

**Fix:** All updated to `py-3 min-h-[44px]`.

---

### 7. `src/shared/ui/InstallBanner.tsx`
**Issue:** Position used `style={{ bottom: 'calc(64px + env(safe-area-inset-bottom))' }}` (inline style).
**Requirement:** Use Tailwind only for fixes.
**Fix:** Replaced inline style with Tailwind arbitrary value: `bottom-[calc(64px+env(safe-area-inset-bottom))]`.

---

## Screens with No Issues Found

| Screen | Notes |
|--------|-------|
| `HomeScreen.tsx` | FAB at `bottom-24` (96 px) sits above nav (60 px + safe area). No change needed. |
| `AudioScreen.tsx` (preset cards) | 2-col grid with `px-5` container, `p-4` cards — badges fit without clipping. |
| `ProgressScreen.tsx` (achievement grid) | 3-col grid `px-5`, text wraps correctly within 84 px cells. |
| `TasksScreen.tsx` | Empty state, FAB, and pool sections all correct. |

---

## Z-Index Stack (verified)

| Layer | z-index | Component |
|-------|---------|-----------|
| BottomNav | 30 | `src/app/BottomNav.tsx` |
| FAB (Add Task) | 30 | `HomeScreen`, `TasksScreen` |
| InstallBanner | 40 | `src/shared/ui/InstallBanner.tsx` |
| Modals / toasts | 50+ | `sonner`, `AddTaskModal` |

InstallBanner (`z-40`) correctly renders above BottomNav (`z-30`). Position: `calc(64px + env(safe-area-inset-bottom))` ≈ 8 px clearance above nav top.

---

## Build & Test Results

```
Build:  ✅ 0 TypeScript errors · 0 Vite errors
Tests:  ✅ 78/78 passing (cn, constants, store, offlineQueue)
Bundle: 98.41 kB gzip (index) — unchanged
```

---

*Commit: `fix(ui): complete UI audit — spacing, empty states, overlays, touch targets`*
