/**
 * features/evening/useCharacterProgress.js
 * Bolt 3.1: Re-export from shared/hooks/ for backwards compatibility.
 * The canonical location is now shared/hooks/useCharacterProgress.js
 * (promoted because PersonaCard also needs it — features can't cross-import).
 */
export { useCharacterProgress } from "../../shared/hooks/useCharacterProgress.js";
