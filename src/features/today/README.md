# Today Feature

Shows the user's tasks marked as "today". AI suggests the top 3 to focus on via `aiFocusSuggest()`. Users can archive completed tasks (triggers streak update) or move items back to the dump.

**AI function:** `aiFocusSuggest()` → Claude Sonnet 4  
**Screen:** `TodayScreen`  
**Status:** Planned for Sprint 1 (code lives in `src/mindflow.jsx` until then)
